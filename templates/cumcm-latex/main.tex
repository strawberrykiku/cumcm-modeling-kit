% !TEX program = xelatex
% ================================================================
% 全国大学生数学建模竞赛 论文模板（匿名版）
% 已对齐《全国大学生数学建模竞赛论文格式规范（2026 年修订稿）》
% ----------------------------------------------------------------
% 编译：XeLaTeX，连编两次（生成页码与交叉引用）
% Overleaf：新建项目 → 上传本文件 → Menu → Compiler 选 XeLaTeX
% ----------------------------------------------------------------
% 模板已固化的官方硬性要求：
%   · A4 纸，上下左右页边距均 2.5 cm
%   · 电子版首页即摘要页；不放承诺书、编号专用页、自制封面、目录
%   · 页码从摘要页起编为第 1 页，置于页脚中部
%   · 摘要含标题与关键词，原则上不超过一页；无需英文摘要
%   · 正文不超过 30 页；附录页数不限
%   · 图题在图下方，表题在表上方，均五号(10.5pt)居中；表格用三线表
%   · 重要公式居中，编号右对齐，正文以“由式(1)可得”引用
%   · “AI 工具使用声明”置于参考文献之前
%   · 附录必须列出支撑材料文件清单
% 匿名要求：全文不得出现姓名、学号、联系方式、指导教师、
%           学校/学院/实验室/赛区名称，或可识别身份的路径与水印
% ================================================================
\documentclass[12pt, a4paper]{ctexart}

% ---------- 页面：四边 2.5 cm ----------
\usepackage[left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm]{geometry}

% ---------- 西文字体：Times New Roman（缺失时用度量兼容的 TeX Gyre Termes）----------
\usepackage{fontspec}
\IfFontExistsTF{Times New Roman}
  {\setmainfont{Times New Roman}}
  {\setmainfont{TeX Gyre Termes}}

% ---------- 行距 1.25 倍、首行缩进 2 字符 ----------
\usepackage{setspace}
\setstretch{1.25}
\setlength{\parindent}{2em}

% ---------- 常用字号（对应中文字号）----------
\newcommand{\xiaoer}{\fontsize{18pt}{22pt}\selectfont}   % 小二 18pt
\newcommand{\sanhao}{\fontsize{16pt}{20pt}\selectfont}   % 三号 16pt
\newcommand{\sihao}{\fontsize{14pt}{18pt}\selectfont}    % 四号 14pt
\newcommand{\xiaosi}{\fontsize{12pt}{15pt}\selectfont}   % 小四 12pt
\newcommand{\wuhao}{\fontsize{10.5pt}{13pt}\selectfont}  % 五号 10.5pt

% ---------- 数学 ----------
\usepackage{amsmath, amssymb, bm}

% ---------- 标题样式：一级黑体三号 / 二级黑体四号 / 三级黑体小四 ----------
% 黑体本身即为粗黑效果，不再叠加 \bfseries（避免伪粗体告警）
\ctexset{
  section/format      = \raggedright\heiti\sanhao,
  section/aftername   = \quad,
  section/beforeskip  = 12pt,
  section/afterskip   = 6pt,
  subsection/format     = \raggedright\heiti\sihao,
  subsection/aftername  = \quad,
  subsection/beforeskip = 6pt,
  subsection/afterskip  = 3pt,
  subsubsection/format     = \raggedright\heiti\xiaosi,
  subsubsection/aftername  = \quad,
  subsubsection/beforeskip = 3pt,
  subsubsection/afterskip  = 0pt,
}

% ---------- 图表题：宋体五号、居中；图题在下、表题在上 ----------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{caption}
\DeclareCaptionFont{songwuhao}{\songti\wuhao}
\captionsetup{font=songwuhao, labelfont=songwuhao, labelsep=quad, justification=centering}
\captionsetup[figure]{name=图, skip=6pt}
\captionsetup[table]{name=表, skip=3pt}

% ---------- 算法伪代码 ----------
\usepackage{algorithm}
\usepackage{algpseudocode}
\floatname{algorithm}{算法}

% ---------- 附录代码：宋体五号等宽 ----------
\usepackage{xcolor}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\wuhao,
  keywordstyle=\color{blue!70!black},
  commentstyle=\color{gray},
  stringstyle=\color{orange!80!black},
  numbers=left, numberstyle=\tiny\color{gray},
  breaklines=true, frame=single, tabsize=4, showstringspaces=false,
}
% 提示：listings 的等宽字体通常缺中文字形，附录代码注释建议用英文。

% ---------- 页脚居中页码 ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\wuhao\thepage}
\renewcommand{\headrulewidth}{0pt}

% ---------- 超链接（放最后加载）----------
\usepackage[hidelinks]{hyperref}

\begin{document}
\pagenumbering{arabic}   % 摘要页即第 1 页

% ==================== 摘要专用页（电子版第一页）====================
% 规范：摘要含标题和关键词，原则上不超过一页；不需要英文摘要。
% 标题要准确概括研究对象和任务，不写成“某年数学建模A题论文”，不含身份信息。
\begin{center}
  {\heiti\xiaoer 基于XXX的XXX模型}
\end{center}
\vspace{18pt}

\begin{center}
  {\heiti\sanhao 摘\quad 要}
\end{center}
\vspace{6pt}

% 摘要写法：总述 + 分问 + 检验 + 总结；每问必须给出关键量化结果。
% 不放公式、图片、表格、参考文献和大段背景。所有结果确定后最后定稿。
针对……问题，本文对……进行研究，完成了……等任务。

对于问题一，首先……，然后……，得到……（给出具体数值结果）。

对于问题二，在前述基础上进一步考虑……，通过……完成求解，结果表明……（给出具体数值结果）。

最后，从……方面对结果进行检验，结果表明……。

\vspace{6pt}
\noindent{\heiti 关键词：}\quad 关键词一；关键词二；关键词三；关键词四

% 规范：正文不要目录。摘要页结束后直接进入正文。
\newpage

% ==================== 正文 ====================
\section{问题重述}

\subsection{问题背景}
（用自己的语言写清背景，不大段复制题目原文。）

\subsection{问题提出}
本文需要解决以下问题：

问题一：……

问题二：……

\section{问题分析}
% 应说明：核心难点、需处理的数据或变量、准备建立的关系、求解顺序和方法、与其他小问的联系。
\subsection{问题一分析}
（这一问的数学本质是什么？为什么采用该方法而不是替代方案？与后续问题的联系？）

\begin{figure}[htbp]
  \centering
  % \includegraphics[width=0.8\textwidth]{figures/flowchart.pdf}
  \caption{整体建模流程图}
  \label{fig:flow}
\end{figure}

如图~\ref{fig:flow} 所示，……。   % 规范：图必须在正文中引用并解释

\subsection{问题二分析}
……

\section{模型假设}
% 假设数量适中、与后文有关、不与题目冲突，并说明忽略因素的理由。
\begin{enumerate}
  \item 假设……。理由：……。
  \item 假设……。理由：……。
\end{enumerate}

\section{符号说明}
% 规范：使用“符号—含义—单位”三线表；同一符号全文含义一致；有单位必须注明。
\begin{table}[htbp]
  \centering
  \caption{符号说明}
  \label{tab:notation}
  {\songti\wuhao
  \begin{tabular}{ccc}
    \toprule
    符号 & 含义 & 单位 \\
    \midrule
    $x_i$ & …… & —— \\
    $T$   & …… & s \\
    \bottomrule
  \end{tabular}}
\end{table}

% 规范：数据处理章按题目需要保留，无数据处理内容可整章删除。
\section{数据处理}
（数据来源、缺失值与异常值处理、标准化等，每种处理都要说明为什么这样做。）

\section{模型的建立与求解}
% 规范：每个小问形成完整闭环——
% 求解思路 → 变量定义 → 关系与约束 → 求解方法 → 结果 → 结果解释
% 公式前说明建立理由，公式后解释符号和用途；不连续堆公式，不只展示软件结果。

\subsection{问题一的建立与求解}

\subsubsection{求解思路}
……

\subsubsection{变量与参数}
……

\subsubsection{模型建立}
根据……，可建立如下关系：
\begin{equation}
  \min\ f(\bm{x}) = \sum_{i=1}^{n} c_i x_i , \qquad \text{s.t.}\ \ g(\bm{x}) \le 0 .
  \label{eq:model1}
\end{equation}
由式~(\ref{eq:model1}) 可知，其中 $c_i$ 表示……，$g(\bm{x})\le 0$ 表示……。

\subsubsection{求解过程}
采用……方法求解，步骤见算法~\ref{alg:1}。

\begin{algorithm}[htbp]
  \caption{……求解算法}
  \label{alg:1}
  \begin{algorithmic}[1]
    \Require ……
    \Ensure ……
    \State 初始化……
    \While{未满足收敛条件}
      \State 更新……
    \EndWhile
    \State \Return 最优解
  \end{algorithmic}
\end{algorithm}

\subsubsection{结果分析}
求解得到……（明确给出题目要求的最终答案与单位）。表~\ref{tab:res1} 表明……，其原因是……。

\begin{table}[htbp]
  \centering
  \caption{问题一求解结果}
  \label{tab:res1}
  {\songti\wuhao
  \begin{tabular}{ccc}
    \toprule
    项目 & 数值 & 单位 \\
    \midrule
    …… & …… & …… \\
    \bottomrule
  \end{tabular}}
\end{table}

\subsection{问题二的建立与求解}
（同上五步结构。）

\section{模型检验}
% 规范：通过已知结果对比、实际数据对比、参数变化、精度变化、交叉验证或误差分析进行检验。
\subsection{结果验证}
……

\subsection{灵敏度分析}
对关键参数施加 $\pm 10\%$、$\pm 20\%$ 扰动，结果表明……，说明模型……。

\section{模型评价}
% 规范：具体说明考虑了什么、忽略了什么、影响是什么、如何改进；
% 不要只写“准确性高、适用性强”这类空泛结论。
\subsection{优点}
（1）……

\subsection{不足}
（1）本模型将……假设为……，未考虑……，可能导致……。

\subsection{改进方向}
（1）……

% 规范：模型推广章按需保留，无内容可整章删除。
\section{模型推广}
……

% ==================== AI 工具使用声明 ====================
% 规范（2026）：必须置于“参考文献”之前。二选一，删除不适用的那句。
% 使用 AI 时，支撑材料中还需包含《AI 工具使用详情.pdf》。
% 正式比赛前请再次核对当届最新规定。
\section*{AI 工具使用声明}
\addcontentsline{toc}{section}{AI 工具使用声明}

本参赛队在竞赛过程中未使用任何 AI 工具。

% 本参赛队在竞赛过程中使用了 AI 工具，主要用于……，详细使用情况见支撑材料。

% ==================== 参考文献 ====================
% 规范：按正文首次出现顺序编号，正文标注与文末条目一一对应；
% 所有引用的论文、书籍、网页、公开数据和他人成果都必须列出。
\section*{参考文献}
\addcontentsline{toc}{section}{参考文献}

{\songti\wuhao
\begin{thebibliography}{99}
  \setlength{\itemsep}{0pt}
  \bibitem{ref1} 作者. 文章名称[J]. 期刊名称, 年份, 卷(期): 页码.
  \bibitem{ref2} 作者. 书名[M]. 出版地: 出版社, 年份.
  \bibitem{ref3} 机构或作者. 网页标题[EB/OL]. 网址, 访问日期.
\end{thebibliography}}

% ==================== 附录 ====================
% 规范：附录必须列出支撑材料文件清单；程序须完整可运行。
% 若没有程序须写明“本论文没有用到程序”；没有支撑材料须写明“本论文没有支撑材料”。
\newpage
\section*{附录}
\addcontentsline{toc}{section}{附录}

\subsection*{附录 A\quad 支撑材料文件清单}

{\songti\wuhao
\begin{tabular}{cll}
  \toprule
  序号 & 文件名 & 说明 \\
  \midrule
  1 & solve\_q1.py & 问题一求解程序 \\
  2 & result1.xlsx & 问题一结果文件 \\
  3 & figures/ & 论文所用高清图片 \\
  \bottomrule
\end{tabular}}

\subsection*{附录 B\quad 问题一求解源代码（Python）}

\begin{lstlisting}[language=Python]
import numpy as np

# Solution code for Problem 1.
# Keep comments in English: the monospace font usually lacks CJK glyphs.
# Variable names should match the symbols used in the paper body.
def solve():
    pass
\end{lstlisting}

\end{document}

